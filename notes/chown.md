# Chown

As a user, you don't always have ownership over a directory. To gain permissions to clone into a new project directory on the server, run the following command in the desired repo directory.

`sudo chown devin:devin /path/to/directory`

Tip: To make a new directory

`sudo mkdir /path/to/directory`
