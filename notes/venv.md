# Venv

### Create a new venv
python3 -m venv venv

### Activate a new venve
source venv/bin/activate

### Deactivate venve
deactivate

### Once the venv has been activated, install project dependencies from requirements.txt file by using
pip install -r requirements.txt